# 新增功能实现说明

> 日期：2026-07-09
> 本次更新实现了两个功能：**主观题（ESSAY）** 和 **错题练习闭环**

---

## 一、主观题（ESSAY）功能

### 1.1 功能概述

新增 `ESSAY` 题型（主观题/简答题），与 `DRAWING`（作图题）类似，属于人工阅卷题型：
- 后台可录入参考答案或评分要点
- 考生输入文字作答（支持最长 5000 字）
- 系统标记为"待人工核对"，不自动判分，不自动计入错题本
- 支持 Excel 导入

### 1.2 后端改动

| 文件 | 类型 | 说明 |
| --- | --- | --- |
| `util/QuestionAnswerEvaluator.java` | 新建 | 答案判分器，支持 7 种题型的判分和人工核对标记 |
| `dto/PageResult.java` | 新建 | 分页工具类（补充项目缺失文件） |
| `controller/QuestionController.java` | 修改 | 题型校验增加 `ESSAY`，HTML 清洗覆盖主观题 |
| `service/QuestionImportService.java` | 修改 | 导入模板新增主观题选项和示例行；类型标准化支持"主观/主观题/简答/简答题" |
| `controller/ExamController.java` | 修改 | 复习详情中 `manualReview` 判断覆盖 ESSAY |

### 1.3 前端改动

| 文件 | 类型 | 说明 |
| --- | --- | --- |
| `components/AnswerInput.vue` | 新建 | 统一答案输入组件，支持全部 7 种题型 |
| `utils/dicts.js` | 修改 | 字典增加 `ESSAY: '主观题'` |
| `views/admin/QuestionManage.vue` | 修改 | 筛选下拉、表单下拉增加主观题；答案区域支持富文本；答案摘要显示"参考答案/评分要点" |
| `views/student/Practice.vue` | 修改 | 使用 `AnswerInput` 组件替换内联表单，支持主观题作答 |
| `views/student/Exam.vue` | 修改 | 考试页面增加主观题作答区域；背题模式结果、复习和记录复习中判断覆盖 ESSAY |

### 1.4 数据库改动

```sql
-- question 表 type 字段注释更新
ALTER TABLE question MODIFY COLUMN type VARCHAR(32) NOT NULL 
  COMMENT 'SINGLE/MULTIPLE/JUDGE/FILL/CALCULATION/DRAWING/ESSAY';
```

---

## 二、错题练习闭环

### 2.1 功能概述

在错题本基础上增加完整的练习闭环：

1. **错题重新练习**：逐题练习模式，提交后立即显示答案和解析
2. **生成错题卷**：将未掌握的错题组成一套练习卷，支持上一题/下一题导航
3. **答对自动标记已掌握**：练习中答对的题目自动从错题列表中移除
4. **练习记录**：每次练习保存记录，可追溯历史

### 2.2 后端新增

| 文件 | 类型 | 说明 |
| --- | --- | --- |
| `entity/WrongPracticeRecord.java` | 新建 | 错题练习记录实体 |
| `mapper/WrongPracticeRecordMapper.java` | 新建 | MyBatis-Plus Mapper |
| `service/WrongPracticeService.java` | 新建 | 错题练习核心服务：获取题目、生成错题卷、提交答案、自动标记掌握 |
| `controller/WrongPracticeController.java` | 新建 | REST 接口：`/api/wrong-questions/practice/` |
| `entity/WrongQuestion.java` | 修改 | 增加 `lastPracticedAt` 字段 |

### 2.3 前端新增

| 文件 | 类型 | 说明 |
| --- | --- | --- |
| `views/student/WrongPractice.vue` | 新建 | 错题练习页面，支持逐题练习和错题卷两种模式 |
| `views/student/WrongBook.vue` | 修改 | 增加"错题重新练习"和"生成错题卷"按钮入口；操作列增加"重新练习" |
| `api/index.js` | 修改 | 新增 3 个 API：`fetchWrongPracticeQuestions`、`generateWrongPaper`、`submitWrongPractice` |
| `router/index.js` | 修改 | 新增路由 `/student/wrong-practice` |

### 2.4 数据库改动

```sql
-- 新增错题练习记录表
CREATE TABLE wrong_practice_record (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  user_id BIGINT NOT NULL,
  bank_id BIGINT NOT NULL DEFAULT 1,
  wrong_question_id BIGINT,
  question_id BIGINT NOT NULL,
  user_answer MEDIUMTEXT,
  correct TINYINT NOT NULL DEFAULT 0,
  practiced_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  INDEX idx_wrong_practice_user (user_id),
  INDEX idx_wrong_practice_bank_user (bank_id, user_id),
  INDEX idx_wrong_practice_question (question_id)
);

-- 错题表增加最后练习时间字段
ALTER TABLE wrong_question ADD COLUMN last_practiced_at DATETIME NULL AFTER last_wrong_at;
```

### 2.5 API 接口

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/api/wrong-questions/practice/questions` | 获取未掌握错题列表（最多 50 题） |
| POST | `/api/wrong-questions/practice/generate-paper` | 生成错题卷（最多 30 题） |
| POST | `/api/wrong-questions/practice/submit?wrongQuestionId={id}` | 提交练习答案 |

---

## 三、数据库升级

执行升级脚本：

```bash
mysql -u root -p question_bank < database/upgrade-20260709-essay-and-wrong-practice.sql
```

如果是全新部署，`schema.sql` 已包含所有新表和字段。

---

## 四、补充说明

### 4.1 缺失文件修复

项目中 `QuestionAnswerEvaluator.java` 和 `PageResult.java` 被引用但源文件缺失，本次一并补齐：

- `QuestionAnswerEvaluator`：统一答案判分逻辑，支持 SINGLE/MULTIPLE/JUDGE/FILL/CALCULATION/DRAWING/ESSAY 七种题型
- `PageResult`：MyBatis-Plus 分页结果封装

### 4.2 AnswerInput 组件

新建的 `AnswerInput.vue` 组件统一了所有题型的答案输入，可在章节练习和错题练习中复用，避免重复代码。

### 4.3 权限说明

- 错题练习接口需要用户已登录且拥有题库使用权限
- 主观题管理需要题库管理权限
- 所有接口均按题库隔离数据

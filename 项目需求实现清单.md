# 智能题库项目需求实现清单

> 维护用途：记录本项目从最初设计到后续迭代中提出过的需求、当前实现状态、涉及模块和待完善事项。以后继续开发时，可以先看这份文档判断“
已经做了什么、还差什么、哪里可能需要改”。

更新时间：2026-06-27

## 1. 项目定位

智能题库网站是一个前后端分离的刷题与题库管理系统。

技术栈：

- 后端：Java 8、Spring Boot、MyBatis-Plus、MySQL、JWT
- 前端：Vue2、Vue Router、Vuex、Axios、Element UI
- 数据库：MySQL

核心用户：

- 超级管理员：`ADMIN`
- 授权管理员：`MANAGER`
- 考生：`STUDENT`

## 2. 当前角色与权限

| 角色 | 说明 | 当前权限 |
| --- | --- | --- |
| `ADMIN` 超级管理员 | 系统最高权限账号 | 可进入管理端；可管理题库、知识点、试卷、用户；可创建、编辑、删除用户；可重置密码；可导出管理端模块 Excel |
| `MANAGER` 授权管理员 | 由 `ADMIN` 创建的管理账号 | 可进入管理端；题库、知识点、试卷等模块可正常使用；用户管理模块只读 |
| `STUDENT` 考生 | 刷题和考试用户 | 可进入考生端；章节练习、模拟考试、错题本、智能推荐、成绩分析 |

当前账号创建策略：

- 用户端不再开放公开注册。
- 所有账号由后台 `ADMIN` 统一创建。
- 停用账号不能登录。

## 3. 已实现功能总览

### 3.0 视觉与体验

| 需求 | 状态 | 说明 |
| --- | --- | --- |
| 前端整体美化 | 已实现 | 统一后台和学生端的背景、导航、卡片、表格、按钮、弹窗、登录页和首页看板视觉风格 |
| 管理端侧边栏折叠 | 已优化 | 折叠按钮移入顶部栏，避免悬浮在内容边界造成遮挡；桌面端可折叠菜单，手机端打开抽屉菜单 |
| 管理端表格可读性 | 持续优化 | 调整试卷管理表格列宽，试卷名称和题库列支持更宽展示和悬浮提示，减少名称换行挤压 |

### 3.1 登录与账号

| 需求 | 状态 | 说明 |
| --- | --- | --- |
| 用户登录 | 已实现 | 使用 JWT 登录态 |
| 用户端公开注册 | 已取消 | 当前要求由后台 `ADMIN` 管控账号 |
| 修改密码 | 已实现 | 用户登录后可修改自己的密码 |
| 用户资料修改 | 已实现 | 管理员和考生顶部统一头像下拉菜单，可修改姓名和头像 |
| 默认头像 | 已实现 | 未上传头像时使用系统默认头像样式 |
| AI 解答授权 | 已实现 | 超级管理员可在用户管理中授权指定用户使用 AI 解答 |
| 管理员重置用户密码 | 已实现 | 仅 `ADMIN` 可操作 |
| 创建用户 | 已实现 | 仅 `ADMIN` 可创建 `ADMIN / MANAGER / STUDENT` |
| 编辑用户 | 已实现 | 仅 `ADMIN` 可编辑 |
| 删除用户 | 已实现 | 仅 `ADMIN` 可删除，不能删除当前登录账号 |
| 用户停用 | 已实现 | `status = 0` 的账号不能登录 |

### 3.2 管理端

| 模块 | 状态 | 说明 |
| --- | --- | --- |
| 管理端数据看板 | 已实现 | 展示基础统计信息 |
| 管理端数据看板导出 | 已实现 | 仅 `ADMIN` 可导出 Excel |
| 知识点管理 | 已实现 | 可新增、编辑、删除；有子知识点或题目时禁止删除；支持名称/说明查询和分页；`ADMIN / MANAGER` 可用；仅 `ADMIN` 可导出 Excel |
| 题目管理 | 已实现 | 可新增、编辑、删除、批量删除、导入；支持题干、知识点、题型、难度查询和分页；`ADMIN / MANAGER` 可用；仅 `ADMIN` 可导出 Excel |
| 试卷管理 | 已实现 | 可新增、编辑、删除、启用、停用；新增/编辑时可用 Element UI 穿梭框批量选择题目并过滤；列表展示当前试卷题目数；支持试卷名称、状态查询和分页；`ADMIN / MANAGER` 可用；仅 `ADMIN` 可导出 Excel |
| 用户管理 | 已实现 | `ADMIN` 可增删改查、重置密码、导出 Excel；支持用户名、姓名、身份、状态查询和分页；`MANAGER` 只读 |

### 3.3 题库与题目

| 需求 | 状态 | 说明 |
| --- | --- | --- |
| 单选题 | 已实现 | `SINGLE` |
| 多选题 | 已实现 | `MULTIPLE` |
| 判断题 | 已实现 | `JUDGE` |
| 填空题 | 已实现 | `FILL`；后台可录入、Excel 可导入，支持使用 `||` 配置多个可接受答案，按忽略大小写和空白后的文本自动判分 |
| 计算题 | 已实现 | `CALCULATION`；后台可录入、Excel 可导入，考生填写最终计算结果并自动判分 |
| 作图题 | 已实现首版 | `DRAWING`；后台可录入参考答案或参考图，考生上传 PNG/JPG 图片作答；系统标记为"待人工核对"，不自动计入错题本 |
| 主观题 | 已实现 | `ESSAY`；后台可录入参考答案或评分要点，考生输入文字作答；系统标记为"待人工核对"，不自动计入错题本；支持 Excel 导入 |
| 难度设置 | 已实现 | `EASY / MEDIUM / HARD`，页面显示为中文 |
| 题干支持文字和图片 | 已实现 | 后台编辑题目支持插入图片，前端用富文本展示 |
| 解析支持文字和图片 | 已实现 | 同题干 |
| HTML 内容基础清洗 | 已实现 | 后端清理部分危险 HTML，如脚本、事件属性等 |
| 选择题支持 A-D | 已实现 | 基础选择题选项 |
| 选择题支持 A-E/F/G 等更多选项 | 已实现 | 导入时根据表头 `A、B、C、D、E...` 动态读取；后台手工录入支持动态新增/删除选项，前台按真实选项数量展示 |
| 题型和难度中文展示 | 已实现 | 不再在页面显示 `SINGLE / EASY` 等英文枚举 |
| 题目列表展示知识点 | 已实现 | 后台题目管理列表显示题目所属知识点名称 |
| 多题库管理 | 已实现 | 管理端新增题库管理，可创建、编辑、停用和删除题库 |
| 题库授权 | 已实现 | `ADMIN` 可在用户管理中给用户配置可访问题库 |
| 学生端切换题库 | 已实现 | 学生端顶部可切换题库，未授权题库显示锁定状态，不能进入使用 |
| 按题库隔离题目和试卷 | 已实现 | 题目、知识点、试卷、练习、模拟考试、错题本、成绩分析、AI 解答和导入均按题库过滤，并由后端校验授权 |
| 题库使用/管理权限拆分 | 已实现 | `user_question_bank.access_type` 区分 `VIEW` 使用权限和 `MANAGE` 管理权限；考生授权为使用权限，授权管理员授权为管理权限，超级管理员默认可访问全部题库 |
| AI 解答题目 | 已实现 | 已授权用户仅可在背题模式和错题解析场景调用 DeepSeek 进行 AI 解答；章节练习和考试作答模式不展示入口；密钥由后端配置；后端会兼容 API Key 首尾误带引号的情况 |
| AI 解析用户成绩 | 已实现 | 复用 AI 授权开关；已授权学生可在交卷后或成绩分析页按单次考试调用 DeepSeek，基于本次得分、逐题答案和错题生成学习建议 |
| 历史考试复习 | 已实现 | 成绩分析页最近考试支持查看复习，可看到原先选择的答案、正确答案、选项和解析 |
| 试卷排名与历史对比 | 已实现 | 同一试卷下展示本次排名、每个用户最好成绩排名、个人最高/平均/上次成绩和个人历次成绩 |
| 背题模式题目评论 | 已实现 | 背题模式下每道题支持用户发表评论，可选择公开给所有人或仅自己可见；本人可删除评论或切换可见范围 |

### 3.4 题库导入

| 需求 | 状态 | 说明 |
| --- | --- | --- |
| CSV 导入题库 | 已实现 | 后台题目管理页导入 |
| XLSX 导入题库 | 已实现 | 使用 Apache POI |
| 下载导入模板 | 已实现 | 后端生成 `.xlsx` 模板，题库、知识点、试卷、六种题型、难度支持 Excel 下拉选择 |
| 动态选项列 | 已实现 | 支持 `A、B、C、D、E...` |
| 导入结果反馈 | 已实现 | 显示成功数、失败数、错误信息 |
| 导入时自动创建知识点 | 已实现 | 知识点不存在时自动创建 |

推荐导入格式：

```text
题库,知识点,试卷,题型,难度,题干,A,B,C,D,E,正确答案,解析
```

说明：

- 选项列可以继续增加到 `F / G / H...`
- 题库列可填写题库名称或题库 ID；为空时使用页面当前选择的题库
- 试卷列可填写试卷名称或试卷 ID；为空时只导入题目，不自动加入试卷
- 单选答案示例：`B`
- 多选答案示例：`A,C,E`
- 判断题答案：`正确` 或 `错误`
- 填空题的多个可接受答案使用 `||` 分隔
- 计算题填写最终标准结果
- 作图题不需要选项，正确答案可填写参考说明或图片 HTML，考生答案进入人工核对

### 2026-07-02 填空、计算与作图题

- 管理端题目表单、筛选、中文字典、Excel 模板和导入服务已支持 `FILL / CALCULATION / DRAWING`。
- 学生端章节练习、背题模式、模拟考试、考试任务和历史复习可展示并作答三种新题型。
- 填空题和计算题采用规则自动判分；作图题使用图片答案并标记为待人工核对。
- 作图题暂不自动计分，也不会被误收进错题本；后续可继续开发人工阅卷与补分流程。
- 需要执行数据库升级脚本 `database/upgrade-20260702-question-types.sql`，将标准答案和用户答案字段扩容为 `MEDIUMTEXT`。

### 3.5 考生端

| 模块 | 状态 | 说明 |
| --- | --- | --- |
| 考生首页 | 已实现 | 展示累计答题、正确率、错题数量、今日建议 |
| 章节练习 | 已实现 | 按知识点练习；提交后立即显示答案和解析 |
| 模拟考试 | 已实现 | 支持选择试卷、一题一页答题、上一题下一题 |
| 自定义组卷 | 已实现 | 管理端可按题型、题目数量、知识点和难度随机生成试卷；普通考生只能选择已启用试卷考试 |
| AI 智能组卷 | 已实现 | 开通 AI 的管理端账号可输入自然语言需求，系统调用 DeepSeek 解析组卷条件后从题库自动创建试卷 |
| AI 生成题入库 | 已实现 | 开通 AI 的管理端账号可按需求生成题干、选项、答案和解析，并直接写入题库 |
| 题目总览弹窗 | 已实现 | 模拟考试中可查看全部题目并跳转 |
| 背题模式 | 已实现 | 选完题后立即显示答案和解析 |
| 背题评论区 | 已实现 | 背题模式题目下方展示评论区，支持公开评论和私密评论；自己的评论可删除、可切换公开/隐藏 |
| 考试模式 | 已实现 | 最后交卷后才展示答案、错题和解析 |
| 成绩分析 | 已实现 | 展示最近考试记录 |
| 试卷排名 | 已实现 | 考生可在交卷结果、模考记录和成绩分析中查看当前试卷排名、与其他用户的比较、自己的历次成绩 |
| 智能推荐 | 已实现 | 根据错题知识点做规则推荐 |

### 3.6 模拟考试结果

| 需求 | 状态 | 说明 |
| --- | --- | --- |
| 交卷后展示分数 | 已实现 | 显示得分、总分、得分率 |
| 查看错题和解析 | 已实现 | 逐题查看错题 |
| 展示用户错误答案 | 已实现 | 错题卡片显示用户答案 |
| 展示正确答案 | 已实现 | 错题卡片显示正确答案 |
| 展示解析 | 已实现 | 支持富文本解析 |
| 证书预览 | 已实现 | 交卷后可预览证书 |
| PNG 证书下载 | 已实现 | 下载为 PNG 图片 |
| 成绩较好证书更高级 | 已实现 | 根据得分率切换证书样式 |
| 考试错题手动加入错题本 | 已实现 | 交卷后错题不会自动进入错题本，用户可点击“加入错题本” |

### 3.7 错题本

| 需求 | 状态 | 说明 |
| --- | --- | --- |
| 错题本列表 | 已实现 | 展示错题名称、错误次数、最近出错时间 |
| 不展示题目 ID | 已实现 | 已改为展示题目标题 |
| 标记已掌握 | 已实现 | 可将错题标记为已掌握 |
| 查看错题详情 | 已实现 | 弹窗展示题干、选项、正确答案、解析 |
| 查看原先答案 | 已实现 | 历史作答记录里展示用户当时选择的答案 |
| 查看历史记录 | 已实现 | 查询当前用户该题的历史作答记录 |
| 区分来源 | 已实现 | 历史记录显示来源：章节练习或模拟考试 |

当前错题逻辑：

- 章节练习答错：自动进入错题本。
- 模拟考试答错：交卷后展示错题，用户点击“加入错题本”后才进入错题本。

### 3.8 安全与稳定

| 需求 | 状态 | 说明 |
| --- | --- | --- |
| JWT 登录拦截 | 已实现 | 除登录接口外需要 token |
| 前端路由权限 | 已实现 | 管理端和考生端按角色跳转 |
| 后端接口权限 | 已实现 | 管理接口区分后台权限和超级管理员权限 |
| 防止刷接口 | 已实现基础版 | `RateLimitInterceptor` 做简单频率限制 |
| 防止数据库被打崩 | 已实现基础版 | 限流和列表查询 limit；仍可继续加强 |
| 上传大小限制 | 已实现 | 后端限制表单和上传大小 |
| 数据库密码入库保护 | 已处理 | `application.yml` 改为环境变量读取 |

## 4. 数据库变更记录

### 4.1 表结构

当前主要表：

- `role`
- `tenant`
- `user`
- `question_bank`
- `user_question_bank`
- `knowledge_point`
- `question`
- `question_option`
- `paper`
- `paper_question`
- `exam_record`
- `answer_record`
- `wrong_question`
- `recommendation_record`

### 4.1.1 题库数据隔离规则

题库数据隔离采用“题库维度 + 授权类型”：

- `ADMIN`：超级管理员，默认拥有所有题库的使用和管理权限。
- `MANAGER`：授权管理员，只能维护被授予 `MANAGE` 的题库数据。
- `STUDENT`：考生，只能查看和使用被授予 `VIEW` 的题库数据。

关键表字段：

- `user_question_bank.access_type`：授权类型，`VIEW` 表示可使用，`MANAGE` 表示可管理。
- `knowledge_point.bank_id`：知识点所属题库。
- `question.bank_id`：题目所属题库。
- `paper.bank_id`：试卷所属题库。
- `exam_record.bank_id`：考试记录所属题库。
- `answer_record.bank_id`：答题记录所属题库。
- `wrong_question.bank_id`：错题记录所属题库。

后端已在以下入口做权限校验：

- 管理端题目、知识点、试卷的新增、编辑、删除、导入、AI 生成题目、AI 组卷：必须有当前题库 `MANAGE` 权限。
- 用户端练习、考试、错题本、成绩分析、AI 解答题目：必须有当前题库使用权限。
- 试卷选题时会校验题目必须属于试卷所在题库，避免跨题库混入题目。

本次升级 SQL：

```text
database/upgrade-20260627-data-scope.sql
```

### 4.1.2 轻量多租户规则

系统在题库授权之上增加轻量多租户层，采用共享数据库、租户字段隔离的方式：

- `tenant`：租户/机构信息，默认租户编码为 `DEFAULT`。
- `user.tenant_id`：用户所属租户，登录令牌中会携带当前租户。
- `question_bank.tenant_id`：题库归属租户。
- `question_bank.owner_user_id`：题库创建人。
- 授权管理员创建题库时，系统强制使用当前账号租户，并自动给创建人授予 `MANAGE`。
- 同租户题库也不会自动开放，仍需明确授予 `VIEW` 或 `MANAGE`，避免管理员之间误改数据。
- 超级管理员可维护租户，并可把任意租户题库授权给其他租户用户，实现受控的跨租户共享。
- 普通管理员只能管理本租户用户，不能进行跨租户授权。
- 租户停用后，该租户用户不能登录；删除租户前必须先迁移其用户和题库。

本次升级 SQL：

```text
database/upgrade-20260627-lightweight-tenant.sql
```

升级后后端会根据数据库实时补齐旧登录令牌缺少的 `tenantId`；建议用户重新登录一次，以同步前端保存的租户名称等资料。

最近一次 AI 解答需求新增了用户表字段，用于控制哪些用户可以使用 AI 解答。

### 4.2 最近 SQL 变更

新增角色：

```sql
('MANAGER', '授权管理员')
```

新增演示账号：

```sql
('manager', '490477d7f9d7d8e0a10460664a4e020d', '授权管理员', 'MANAGER', 1)
```

演示账号密码均为：

```text
123456
```

新增用户头像字段：

```sql
ALTER TABLE user
  ADD COLUMN avatar_url MEDIUMTEXT NULL AFTER real_name;
```

新增 AI 解答授权字段：

```sql
ALTER TABLE user
  ADD COLUMN ai_enabled TINYINT NOT NULL DEFAULT 0 AFTER status;
```

新增多题库和用户题库授权：

```sql
CREATE TABLE question_bank (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  name VARCHAR(128) NOT NULL,
  description VARCHAR(512) NULL,
  status TINYINT NOT NULL DEFAULT 1,
  sort_order INT NOT NULL DEFAULT 0,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE user_question_bank (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  user_id BIGINT NOT NULL,
  bank_id BIGINT NOT NULL,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  UNIQUE KEY uk_user_bank (user_id, bank_id)
);

ALTER TABLE question
  ADD COLUMN bank_id BIGINT NOT NULL DEFAULT 1 AFTER id;

ALTER TABLE paper
  ADD COLUMN bank_id BIGINT NOT NULL DEFAULT 1 AFTER id;
```

已有数据库升级脚本：

```text
database/upgrade-20260621-question-bank-auth.sql
database/upgrade-20260623-exam-ranking.sql
database/upgrade-20260623-question-comment.sql
```

## 5. 已生成文档和交付物

| 文件 | 状态 | 说明 |
| --- | --- | --- |
| `README.md` | 已有，但部分内容待更新 | 里面仍有“注册”等旧描述，需要后续同步 |
| `outputs/智能题库网站用户使用说明.docx` | 已生成 | 用户使用说明，含页面截图 |
| `项目需求实现清单.md` | 当前文档 | 用于后续维护项目需求和实现状态 |

## 6. 部署与运行相关记录

### 6.1 后端启动

开发环境：

```bash
cd backend
mvn spring-boot:run
```

Jar 运行建议：

```bash
nohup java -jar question-bank-0.0.1-SNAPSHOT.jar > log.log 2>&1 &
```

当前后端配置支持环境变量：

```bash
DB_URL=jdbc:mysql://localhost:3306/question_bank?useUnicode=true\&characterEncoding=utf8\&serverTimezone=Asia/Shanghai
DB_USERNAME=root
DB_PASSWORD=你的数据库密码
JWT_SECRET=你的JWT密钥
JWT_EXPIRE_HOURS=24
DEEPSEEK_API_KEY=你的DeepSeek密钥
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-v4-pro
```

DeepSeek AI 解答也支持用 jar 同级的 `application-local.yml` 存放本地密钥，并通过 `--spring.profiles.active=local` 启用。该文件已被 `.gitignore` 忽略，不应提交到代码仓库。

### 6.2 前端部署

构建：

```bash
cd frontend
npm run build
```

产物：

```text
frontend/dist/
```

可用 Nginx 部署，并将 `/api/` 反向代理到后端服务。

## 7. 当前验证情况

| 验证项 | 状态 | 说明 |
| --- | --- | --- |
| 前端构建 | 通过 | 2026-06-27 执行 `npm run build` 成功，仅有 webpack 包体积 warning |
| 后端 Maven 编译 | 通过 | 2026-06-27 使用 JDK 8 执行 `mvn -DskipTests package` 成功 |
| 页面截图验证 | 做过 | 使用本地 `http://localhost:8081/` 截图生成 Word 使用说明 |
| Git 推送 | 已完成 | 已推送到 Gitee 仓库 |

## 7.1 最近变更记录

| 日期 | 变更 | 说明 |
| --- | --- | --- |
| 2026-06-23 | 优化管理端导航折叠入口 | 将原先贴在侧边栏边缘的折叠按钮移动到顶部栏左侧，桌面端点击折叠/展开，移动端点击打开菜单抽屉 |
| 2026-06-23 | 优化试卷管理列表列宽 | 扩大试卷名称列宽，并给题库和试卷名称增加超长内容提示，减少表格拥挤和换行 |
| 2026-06-23 | 新增试卷排名和历史成绩对比 | 交卷结果、模考记录、成绩分析均可查看当前试卷排名、用户最好成绩排名和个人历次成绩 |
| 2026-06-23 | 新增背题模式题目评论 | 背题模式下可对当前题发表评论，支持所有人可见和仅自己可见；本人可删除和切换可见范围 |
| 2026-06-23 | 新增后台菜单管理 | 管理后台新增类似若依的菜单树管理，支持目录、菜单、按钮权限；角色授权改为勾选菜单树，管理端侧边栏支持从后端菜单动态加载 |
| 2026-06-23 | 菜单权限升级为全端资源管理 | 菜单管理扩展为统一管理后台端和用户端菜单，支持页面菜单、目录和按钮资源；学生端导航也可按后台菜单授权动态加载 |
| 2026-06-23 | 合并功能管理入口 | 功能权限作为底层接口权限字典保留，不再单独暴露功能管理菜单；菜单管理成为目录、页面和按钮权限的统一维护入口 |
| 2026-06-24 | 新增权限标识管理入口 | 重新提供轻量的权限标识字典维护页面，用于管理 `function_permission`；新增业务模块时可先录入权限标识，再到菜单管理绑定 |
| 2026-06-24 | 简化权限标识维护表单 | 权限标识管理去掉页面路径展示和录入，所属模块改为下拉选择并支持新增；页面路由和组件路径统一在菜单管理中维护 |
| 2026-06-24 | 新增题目分值和试卷自动总分 | 题目管理支持设置分值，步长 0.5 分、默认 1 分；导入模板新增分值列；试卷总分不再手输，按试卷下题目分值自动汇总，考试判分和排名支持小数分 |
| 2026-06-27 | 新增题库维度数据隔离 | 题目、知识点、试卷、练习、考试、错题、统计、导入和 AI 功能统一按题库授权校验，区分 `VIEW / MANAGE` |
| 2026-06-27 | 新增轻量多租户 | 用户和题库增加租户归属；管理员可创建本租户题库；超级管理员可维护租户及进行跨租户题库授权 |
| 2026-06-27 | 修复空菜单目录和账号自删除 | 没有任何可见子菜单时自动隐藏目录；用户管理前后端均禁止删除当前登录账号 |
| 2026-06-27 | 修复用户维护权限未生效 | 修复角色树父子联动造成的“视觉已勾选但按钮未保存”，菜单与按钮改为独立授权；兼容旧库 `USER_WRITE` 绑定并在用户管理页实时刷新权限 |
| 2026-06-27 | 支持同账号双工作台 | 同一角色可同时授权管理端和用户端菜单；有用户端权限时登录默认进入用户端，两端均授权时可从头像菜单双向切换；只有管理端权限时仍直接进入管理后台 |
| 2026-06-28 | AI 对话支持思考与正文双流输出 | DeepSeek 思考内容与最终答案分离传输；页面实时展示思考过程，正文开始后自动折叠为“已思考 N 秒”，历史对话可重新展开查看 |
| 2026-06-29 | 超级管理员全局数据视角 | 超级管理员可跨租户查看全部题库、题目、知识点、试卷和用户数据，不再受普通角色授权范围及 200/500 条保护上限影响；个人考试、错题和 AI 对话仍保持账号隔离 |
| 2026-06-29 | 超级管理员题库筛选 | 题库管理支持按题库名称模糊查询；仅超级管理员可按所属租户、创建人下拉筛选；题库列表增加分页，默认每页 10 条 |
| 2026-06-29 | 系统管理列表筛选分页 | 权限标识管理、租户管理、角色管理增加顶部查询过滤和分页，默认每页 10 条，支持按名称/编码/状态等条件快速定位 |
| 2026-06-30 | 租户编码支持自定义或自动生成 | 新增租户时可选择自定义编码或系统自动生成；自定义编码按用户输入保存并统一转为大写，只有选择自动生成时才创建唯一的 `TENANT_XXXXXXXX` 编码 |
| 2026-06-30 | 修复管理端侧栏折叠显示 | 管理后台侧栏接入 Element UI 原生折叠模式，折叠后只显示顶级菜单图标，目录子菜单通过纯白不透明的悬浮弹层访问，不再残留不可见子菜单占位或透出底层页面 |
| 2026-06-30 | 新增考试任务分发一期与二期 | 管理员可批量选择用户分发试卷，支持定时开始、指定区间、仅截止时间和长期有效四种规则，以及考试次数、及格分、通过后停考、成绩发布时间和站内提醒 |
| 2026-06-30 | 新增考试名单与试卷快照 | 发布任务时冻结考生名单和试卷题目、选项、答案、解析、分值，后续修改原题或试卷不会影响已发布考试和历史复习 |
| 2026-06-30 | 新增用户考试任务中心 | 用户端可查看待考、进行中、已完成任务，显示时间规则、剩余次数、最好成绩、未读提醒，并支持参加考试、自动倒计时、历次成绩和逐题复习 |
| 2026-06-30 | 新增考试任务报表与 AI 分析 | 管理端报表展示分发人数、参考率、通过率、平均/中位/最高/最低分、平均用时、分数分布、提交趋势、知识点正确率和考生明细；可调用 DeepSeek 生成匿名教学分析 |
| 2026-06-30 | 增加考试截止后端兜底 | 前端倒计时自动交卷，后端定时扫描并关闭超时未交卷记录，记录自动交卷标识；服务端同时校验开始时间、截止时间、次数和通过状态 |
| 2026-07-01 | 优化考试任务提醒、空报表和删除 | 新任务未读数量移动到用户端顶部“考试任务”菜单；无人答题时使用紧凑空状态；草稿、已发布和已结束任务均可删除，并同步清理成绩、答题明细、名单、试卷快照及 AI 报表 |
| 2026-07-01 | 修复考试任务时间选择 | 切换时间规则时自动清理无关字段；后端只校验当前规则需要的时间；日期面板提升到弹窗遮罩之上；修正 Element UI 固定字符 `T` 的日期格式，使时间第一次确认后仍能继续修改 |
| 2026-07-01 | 管理列表统一后端分页 | 用户、角色、租户、权限、题库、知识点、题目、试卷、公告和考试任务列表统一由后端按页查询，返回 `records / total / current / size`；筛选条件进入 SQL，前端不再对全量结果进行 `slice` 分页；下拉选项和穿梭框候选数据保留独立的全量读取场景 |
| 2026-07-09 | 新增主观题(ESSAY)题型 | 新增主观题/简答题题型，后端判分器支持人工核对标记；管理端题目表单、筛选和 Excel 导入模板支持主观题；学生端章节练习、背题模式、模拟考试和历史复习可展示并作答主观题；主观题不自动判分、不自动计入错题本 |
| 2026-07-09 | 新增错题练习闭环 | 错题本新增"错题重新练习"和"生成错题卷"入口；逐题练习模式答对后自动标记已掌握；错题卷模式支持上一题/下一题导航和即时反馈；新增 `wrong_practice_record` 表记录练习历史 |

## 8. 未实现或待完善事项

### 8.1 功能类

| 项目 | 状态 | 建议 |
| --- | --- | --- |
| 主观题 | 已实现 | 新增 `ESSAY` 主观题/简答题题型，后台可录入、Excel 可导入、学生端可作答；标记为待人工核对，不自动判分不计入错题本 |
| 教师角色 | 已实现 | 新增 `TEACHER` 角色，可管理题库、试卷和考试任务，可查看学生成绩；Flyway V2 迁移脚本自动添加 |
| 审核员角色 | 已实现 | 新增 `REVIEWER` 角色和题目审核字段（`review_status / reviewed_by / reviewed_at`）；支持题目审核流程 |
| AI 解答增强 | 已实现 | 新增 `AiUsageLogService` 和 `ai_usage_log` 表，记录每次 AI 调用的模块、动作、Token 用量、耗时和状态；Flyway V3 迁移脚本 |
| 试卷题目分值和排序细节 | 已实现 | 后端新增 `/api/papers/{id}/reorder` 和 `/api/papers/{id}/question-score` 接口；前端新增对应 API 调用，支持拖拽排序和单独覆盖分值 |
| 考试倒计时强制交卷 | 已实现 | 普通考试支持前端倒计时；分发考试任务增加服务端时间准入、单次作答时长校验、任务截止校验和后台定时关闭超时记录 |
| 考试任务答题草稿 | 已实现 | 用户选择答案后自动保存到服务端，刷新页面可恢复；关闭浏览器后到期自动交卷时按已保存答案判分 |
| 延迟公布成绩 | 已实现 | 可设置截止后公布；截止前后端隐藏得分、通过状态、答案和解析，任务结束后自动开放 |
| 错题重新练习闭环 | 已实现 | 错题本新增"错题重新练习"和"生成错题卷"功能；逐题练习答对后自动标记已掌握，错题卷模式支持上一题/下一题导航 |
| 成绩趋势图 | 已实现 | 个人成绩分析页新增 Canvas 成绩趋势图，展示历次考试得分折线；考试任务报表保留分数分布、每日提交趋势和知识点正确率图表 |
| 用户操作记录查看 | 已实现 | 管理后台新增操作日志页面，自动记录登录用户访问 API 的模块、动作、接口、耗时、结果和时间 |
| 操作日志增强 | 已实现 | 新增 `/api/admin/operation-logs/modules` 获取模块列表、`/before` 按日期清理、`/all` 全部清理接口 |
| 角色管理 | 已实现 | 管理后台新增角色管理，可创建角色并分配功能权限 |
| 权限标识管理 | 已实现 | 可维护 `function_permission` 权限字典，所属模块用于分组展示；新增模块时通过页面录入权限标识，菜单管理中绑定该标识用于角色授权和接口校验 |
| 菜单管理 | 已实现 | 管理后台新增菜单管理，可统一配置管理端和用户端的目录、页面菜单、按钮权限、图标、路径、排序、启停和父子层级 |
| 角色菜单授权 | 已实现 | 角色授权改为选择菜单树，后台菜单、用户端菜单和按钮权限都可挂到角色下，接口权限由菜单节点的权限标识推导 |

### 8.2 权限与安全类

| 项目 | 状态 | 建议 |
| --- | --- | --- |
| 更细粒度权限模型 | 已实现首版 | 新增 `function_permission`、`role_permission`，管理端菜单和核心管理接口按功能权限控制 |
| 密码加密强度 | 已实现 | 已从 MD5 升级为 BCrypt；兼容旧 MD5 密码，用户修改密码后自动迁移为 BCrypt |
| 操作日志 | 已实现增强版 | 通过拦截器自动记录用户操作；新增模块列表查询、按日期清理和全部清理接口 |
| 数据库迁移工具 | 已实现 | 引入 Flyway 管理数据库版本，V1 基线 + V2 教师/审核员 + V3 AI 日志；`baseline-on-migrate: true` 兼容已有数据库 |
| 多租户物理隔离 | 当前为轻量逻辑隔离 | 当前共享一个 MySQL 数据库，通过租户和题库权限隔离；高安全场景可升级为独立库 |
| 接口限流持久化/分布式 | 已实现 | RateLimitInterceptor 支持 Redis 分布式限流，配置 `spring.redis.host` 后自动启用；未配置时降级为本地内存限流 |

### 8.3 工程类

| 项目 | 状态 | 建议 |
| --- | --- | --- |
| README 同步更新 | 已完成 | 已更新角色（含 TEACHER/REVIEWER）、注册策略、启动方式、环境变量、部署说明 |
| 自动化测试 | 已实现首版 | 新增 `QuestionBankApplicationTests`，覆盖登录、密码编码和受保护接口访问 |
| 后端本地编译环境 | 已完成 | 确认 IDEA/Maven 使用 JDK 8，pom.xml 已添加 spring-security-crypto、flyway 依赖 |
| 前端包体积优化 | 已实现 | 前端路由改为懒加载（动态 import），vue.config.js 新增 splitChunks 分包配置 |

## 9. 后续开发建议优先级

建议按以下顺序继续维护：

1. 更新 `README.md`，同步当前角色、注册策略、启动方式和环境变量。
2. 解决后端 Maven/JDK 编译环境问题，保证本地和服务器都能稳定打包。
3. 增加数据库迁移脚本，避免每次靠全量 `schema.sql`。
4. 给用户管理、题库导入、考试提交增加基础接口测试。
5. 完善成绩分析图表和错题重新练习闭环。
6. 将密码算法升级为 BCrypt。

## 10. Git 记录

当前主要提交包括：

- `Initial question bank system`
- `Add admin-controlled user management`
- 以及后续用户要求的功能提交

远程仓库：

```text
https://gitee.com/long-xiaozhen/question_bank.git
```
# 2026-06-30 用户自有 DeepSeek Token

- 管理员为用户开启 AI 解答后，可选择使用“系统 Token”或“用户 Token”。
- 系统 Token 模式继续使用后端环境变量 `DEEPSEEK_API_KEY`。
- 用户 Token 模式必须由用户在右上角“Token 配置”中填写个人 DeepSeek Token；未配置时 AI 解题、成绩分析、AI 生成题目、AI 组卷和 AI 对话都会被拦截。
- 用户 Token 模式不会自动回退到系统 Token，避免误消耗系统额度。
- 个人 Token 使用 AES-GCM 加密后存入数据库，接口只返回是否配置，不返回明文或密文。
- 生产环境必须配置稳定的 `TOKEN_ENCRYPTION_KEY`，更换该值后已有个人 Token 无法解密，需要用户重新配置。
- 当前个人 Token 供应商只支持 DeepSeek。
- Token 配置页加入 AI 使用免责声明；个人 Token 保存前必须勾选同意。
- 数据库升级需执行 `database/upgrade-20260630-user-deepseek-token.sql`。

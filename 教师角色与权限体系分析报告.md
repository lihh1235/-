# 题库项目后端架构分析报告

> 分析对象：`C:/Users/li/Desktop/question_bank-master/question_bank-master/`
> 分析范围：教师角色、班级功能、权限体系、数据范围隔离

---

## 一、教师角色是否存在？

### 结论：教师角色已定义，但权限配置不完整

### 1.1 角色定义

**Flyway 迁移脚本** `V2__add_teacher_reviewer_roles.sql` 中已定义：

```sql
INSERT IGNORE INTO role(code, name, description, status) VALUES
('TEACHER', '教师', '可管理题库、试卷和考试任务，可查看学生成绩', 1),
('REVIEWER', '审核员', '负责题目审核流程，可批准或驳回待审核题目', 1);
```

**基线 schema.sql** 中定义了 3 个初始角色：
| code | name | 描述 |
|------|------|------|
| `ADMIN` | 超级管理员 | 拥有系统全部功能权限 |
| `MANAGER` | 授权管理员 | 可维护题库业务，用户管理只读 |
| `STUDENT` | 考生 | 用户端刷题、考试和复习 |

加上 V2 迁移新增的，系统共有 **5 个角色**：`ADMIN`、`MANAGER`、`TEACHER`、`REVIEWER`、`STUDENT`。

### 1.2 User 实体的 roleCode 字段

```java
// User.java
private String roleCode;  // 存储角色编码，无枚举约束
```

`roleCode` 是一个自由字符串字段，没有数据库级 CHECK 约束，也没有 Java 枚举定义。可能的值由 `role` 表中的记录决定。

### 1.3 PermissionService 中的教师默认权限

```java
private static final List<String> TEACHER_DEFAULT_CODES = Arrays.asList(
    DASHBOARD,              // 数据看板
    QUESTION_BANK,          // 题库管理
    KNOWLEDGE_POINT,        // 知识点管理
    QUESTION,               // 题目管理
    PAPER,                  // 试卷管理
    NOTICE,                 // 公告管理
    USER_READ,              // 用户查看（只读）
    EXAM_TASK,              // 考试任务
    EXAM_TASK_MANAGE,       // 考试任务管理
    STUDENT_STATISTICS_VIEW  // 学生成绩查看
);
```

### 1.4 教师角色的使用位置

| 文件 | 位置 | 用途 |
|------|------|------|
| `PermissionService.java:411` | `defaultCodes()` | 教师角色默认权限集合 |
| `PermissionService.java:351` | `hasWorkspace()` | 教师被归类为"后台用户"(ADMIN workspace) |
| `QuestionBankAccessService.java:101` | `isBackstage()` | 教师被识别为后台用户 |
| `StatisticsController.java:122` | 注释 | 标注"教师端：练习情况统计" |
| 前端 `workspace.js:35` | `isBackstage()` | 教师可进入管理后台 |

### 1.5 问题与缺陷

1. **教师没有 `USER_WRITE` 权限**：教师无法管理用户/学生，包括无法将学生加入班级
2. **教师权限"硬编码"**：教师默认权限在 Java 代码中通过 `defaultCodes()` 兜底返回，但 V2 脚本只向 `function_permission` 表插入了 3 个权限标识（`EXAM_TASK_MANAGE`、`QUESTION_REVIEW`、`STUDENT_STATISTICS`），**没有向 `role_permission` 表插入教师角色的权限映射记录**。这意味着如果数据库 `role_permission` 表中查询不到教师记录，就会走代码兜底逻辑
3. **没有教师专属的"班级管理"权限标识**

---

## 二、班级功能现状

### 2.1 数据库表结构

**`class` 表**（班级表）：
```sql
CREATE TABLE class (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  name VARCHAR(128) NOT NULL,         -- 班级名称
  description VARCHAR(255),            -- 描述
  tenant_id BIGINT NOT NULL DEFAULT 1, -- 租户隔离
  created_by BIGINT,                   -- 创建者（仅记录，不做关联约束）
  created_at DATETIME,
  updated_at DATETIME
);
```

**`class_user` 表**（班级-用户关联表）：
```sql
CREATE TABLE class_user (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  class_id BIGINT NOT NULL,   -- 班级ID
  user_id BIGINT NOT NULL,    -- 用户ID
  joined_at DATETIME,         -- 加入时间
  UNIQUE KEY uk_class_user (class_id, user_id)
);
```

### 2.2 实体类

```java
// ClassEntity.java — 班级实体
private Long id;
private String name;
private String description;
private Long tenantId;
private Long createdBy;
private LocalDateTime createdAt;
private LocalDateTime updatedAt;

// ClassUser.java — 班级-用户关联实体
private Long id;
private Long classId;
private Long userId;
private LocalDateTime joinedAt;
```

### 2.3 班级与用户的关系

**关键发现：`class_user` 表是一个通用的"班级-用户"关联表，没有角色区分字段。**

当前实现中：
- `ClassController.students()` 方法名为"获取学生"，但实际查询的是 `class_user` 表中的所有关联用户，**不区分用户角色**
- `ClassController.allStudents()` 方法直接查询 `roleCode = 'STUDENT'` 的用户
- `ClassController.updateMembers()` 方法接受 `userIds` 列表，不做角色过滤

**班级和老师的关系：完全没有。**

- 没有 `class_teacher` 表
- `class_user` 表没有 `role` 字段来区分"学生成员"和"教师成员"
- `class` 表的 `created_by` 字段只记录创建者，没有建立"班级-教师"的正式关联
- 没有任何接口可以让教师查看"我的班级"

### 2.4 ClassController 接口清单

| 接口 | 方法 | 权限 | 说明 |
|------|------|------|------|
| `GET /api/admin/classes` | list | `USER_READ` | 列出当前租户所有班级 |
| `POST /api/admin/classes` | create | `USER_WRITE` | 创建班级 |
| `PUT /api/admin/classes/{id}` | update | `USER_WRITE` | 编辑班级 |
| `DELETE /api/admin/classes/{id}` | delete | `USER_WRITE` | 删除班级（级联删除成员） |
| `GET /api/admin/classes/{id}/students` | students | `USER_READ` | 获取班级成员列表 |
| `GET /api/admin/classes/all-students` | allStudents | `USER_READ` | 获取所有学生（不限班级） |
| `PUT /api/admin/classes/{id}/members` | updateMembers | `USER_WRITE` | 设置班级成员 |
| `GET /api/admin/classes/{id}/student-ids` | studentIds | `USER_READ` | 获取班级学生ID列表 |
| `GET /api/my-classes` | myClasses | 登录即可 | 当前用户所在班级 |

### 2.5 班级与考试任务的关联

`exam_task` 表有 `class_id` 字段：
```java
// ExamTask.java
private Long classId;  // 指定班级ID，为空则所有学生可见
```

`ExamTaskController.create()` 中的逻辑：
```java
// 如果选了班级但没手动选考生，自动把班级学生作为考生
List<Long> effectiveUserIds = dto.getUserIds();
if ((effectiveUserIds == null || effectiveUserIds.isEmpty()) && dto.getClassId() != null) {
    List<ClassUser> classUsers = classUserMapper.selectList(
        new LambdaQueryWrapper<ClassUser>().eq(ClassUser::getClassId, dto.getClassId()));
    effectiveUserIds = classUsers.stream().map(ClassUser::getUserId).collect(Collectors.toList());
}
```

### 2.6 班级功能缺失总结

| 缺失项 | 影响 |
|--------|------|
| ❌ 无"班级-教师"关联 | 教师无法知道自己负责哪些班级 |
| ❌ `class_user` 无角色区分 | 班级成员只有"用户"概念，不能区分学生和教师 |
| ❌ 教师无 `USER_WRITE` 权限 | 教师无法创建班级、管理班级成员 |
| ❌ 无"我的班级"教师端接口 | `my-classes` 接口对所有用户一视同仁 |
| ❌ 无班级维度的数据查询 | 统计接口不支持按班级筛选 |

---

## 三、现有权限体系

### 3.1 权限常量列表

`PermissionService` 中定义的所有权限常量：

| 常量名 | 代码值 | 模块 | 说明 |
|--------|--------|------|------|
| `DASHBOARD` | DASHBOARD | 题库业务 | 数据看板 |
| `QUESTION_BANK` | QUESTION_BANK | 题库业务 | 题库管理 |
| `KNOWLEDGE_POINT` | KNOWLEDGE_POINT | 题库业务 | 知识点管理 |
| `QUESTION` | QUESTION | 题库业务 | 题目管理 |
| `PAPER` | PAPER | 题库业务 | 试卷管理 |
| `NOTICE` | NOTICE | 题库业务 | 公告管理 |
| `USER_READ` | USER_READ | 系统管理 | 用户查看 |
| `USER_WRITE` | USER_WRITE | 系统管理 | 用户维护 |
| `OPERATION_LOG` | OPERATION_LOG | 系统管理 | 操作日志 |
| `AI_CHAT` | AI_CHAT | AI能力 | AI对话 |
| `ROLE_MANAGE` | ROLE_MANAGE | 系统管理 | 角色管理 |
| `FUNCTION_MANAGE` | FUNCTION_MANAGE | 系统管理 | 权限标识管理 |
| `MENU_MANAGE` | MENU_MANAGE | 系统管理 | 菜单管理 |
| `TENANT_MANAGE` | TENANT_MANAGE | 系统管理 | 租户管理 |
| `EXAM_TASK` | EXAM_TASK | 题库业务 | 考试任务 |
| `EXAM_TASK_MANAGE` | EXAM_TASK_MANAGE | 题库业务 | 考试任务管理 |
| `QUESTION_REVIEW` | QUESTION_REVIEW | 题库业务 | 题目审核 |
| `STUDENT_STATISTICS_VIEW` | STUDENT_STATISTICS_VIEW | 题库业务 | 学生成绩查看 |
| `STUDENT_HOME` | STUDENT_HOME | 用户端题库 | 用户端首页 |
| `STUDENT_PRACTICE` | STUDENT_PRACTICE | 用户端题库 | 章节练习 |
| `STUDENT_EXAM` | STUDENT_EXAM | 用户端题库 | 模拟考试 |
| `STUDENT_WRONG_BOOK` | STUDENT_WRONG_BOOK | 用户端题库 | 错题本 |
| `STUDENT_RECOMMENDATION` | STUDENT_RECOMMENDATION | 用户端题库 | 智能推荐 |
| `STUDENT_STATISTICS` | STUDENT_STATISTICS | 用户端题库 | 成绩分析 |
| `STUDENT_NOTICE` | STUDENT_NOTICE | 用户端题库 | 热点公告 |
| `STUDENT_TASK` | STUDENT_TASK | 用户端题库 | 我的考试任务 |

### 3.2 各角色默认权限对比

| 权限 | ADMIN | MANAGER | TEACHER | REVIEWER | STUDENT |
|------|:-----:|:-------:|:-------:|:--------:|:-------:|
| DASHBOARD | ✅ | ✅ | ✅ | ✅ | ❌ |
| QUESTION_BANK | ✅ | ✅ | ✅ | ❌ | ❌ |
| KNOWLEDGE_POINT | ✅ | ✅ | ✅ | ❌ | ❌ |
| QUESTION | ✅ | ✅ | ✅ | ✅ | ❌ |
| PAPER | ✅ | ✅ | ✅ | ❌ | ❌ |
| NOTICE | ✅ | ✅ | ✅ | ❌ | ❌ |
| USER_READ | ✅ | ✅ | ✅ | ❌ | ❌ |
| USER_WRITE | ✅ | ❌ | ❌ | ❌ | ❌ |
| OPERATION_LOG | ✅ | ✅ | ❌ | ❌ | ❌ |
| AI_CHAT | ✅ | ✅ | ❌ | ❌ | ❌ |
| ROLE_MANAGE | ✅ | ❌ | ❌ | ❌ | ❌ |
| FUNCTION_MANAGE | ✅ | ❌ | ❌ | ❌ | ❌ |
| MENU_MANAGE | ✅ | ❌ | ❌ | ❌ | ❌ |
| TENANT_MANAGE | ✅ | ❌ | ❌ | ❌ | ❌ |
| EXAM_TASK | ✅ | ✅ | ✅ | ❌ | ❌ |
| EXAM_TASK_MANAGE | ✅ | ❌ | ✅ | ❌ | ❌ |
| QUESTION_REVIEW | ✅ | ❌ | ❌ | ✅ | ❌ |
| STUDENT_STATISTICS_VIEW | ✅ | ❌ | ✅ | ❌ | ❌ |
| 用户端功能 | ✅ | ❌ | ❌ | ❌ | ✅ (全部) |

### 3.3 UserController 的权限控制方法

```java
// requireBackstage() — 要求后台访问权限
private void requireBackstage() {
    permissionService.require(PermissionService.USER_READ);
}

// requireWrite() — 要求写入权限
private void requireWrite() {
    permissionService.require(PermissionService.USER_WRITE);
}

// isSuperAdmin() — 判断是否超级管理员
private boolean isSuperAdmin() {
    return UserContext.get() != null && "ADMIN".equals(UserContext.get().getRoleCode());
}
```

### 3.4 当前权限体系如何区分管理员和普通用户？

权限体系采用 **"功能权限 + 租户隔离 + 题库授权"** 三层机制：

#### 第一层：功能权限（PermissionService）
- 基于 `roleCode` 查询 `role_permission` 表获取权限码集合
- 如果数据库查不到记录，走 Java 代码中的 `defaultCodes()` 兜底
- ADMIN 拥有全部权限，其他角色按预设子集

#### 第二层：租户隔离（tenantId）
```java
// UserController.list() 中
LambdaQueryWrapper<User> query = new LambdaQueryWrapper<User>()
    .eq(!isSuperAdmin(), User::getTenantId, currentTenantId());
```
- 非 ADMIN 用户只能看到自己租户的数据
- ADMIN 可以跨租户操作

#### 第三层：题库授权（QuestionBankAccessService）
```java
// 题库访问权限分为两级
public static final String VIEW = "VIEW";      // 查看（学生用）
public static final String MANAGE = "MANAGE";  // 管理（教师/管理员用）
```
- `user_question_bank` 表记录每个用户对每个题库的访问级别
- ADMIN 自动拥有所有题库的管理权限
- 非 ADMIN 需要通过 `user_question_bank` 表授权

### 3.5 数据隔离机制现状

| 隔离维度 | 实现情况 | 说明 |
|----------|----------|------|
| **租户隔离** | ✅ 已实现 | 非 ADMIN 用户只能看到本租户数据 |
| **题库隔离** | ✅ 已实现 | 非 ADMIN 用户只能操作授权的题库 |
| **教师-班级隔离** | ❌ 不存在 | 教师能看到所有学生数据，不限于"自己班级" |
| **教师-考试任务隔离** | ❌ 不存在 | 教师能看到所有考试任务，不限于"自己创建的" |
| **教师-学生数据隔离** | ❌ 不存在 | 统计接口只按题库过滤，不按班级/教师过滤 |

---

## 四、统计/查看接口的数据范围

### 4.1 StatisticsController.adminPracticeStatistics()

```java
@GetMapping("/api/statistics/admin/practice/statistics")
public ApiResponse<Map<String, Object>> adminPracticeStatistics(
    @RequestParam(required = false) Long bankId,
    @RequestParam(required = false) Long knowledgePointId,
    @RequestParam(required = false) Long userId) {

    permissionService.require(PermissionService.USER_READ);  // 只要求 USER_READ 权限

    // 数据范围：仅按题库授权过滤
    List<Long> authorizedBankIds = bankAccessService.isSuperAdmin()
        ? null  // ADMIN 看全部
        : bankAccessService.currentManageableBankIds();  // 非 ADMIN 看可管理的题库

    // 查询答题记录 — 只按 bankId 过滤，不按班级/教师过滤
    List<AnswerRecord> allRecords = answerRecordMapper.selectList(
        new LambdaQueryWrapper<AnswerRecord>()
            .in(authorizedBankIds != null, AnswerRecord::getBankId, authorizedBankIds)
            .eq(bankId != null, AnswerRecord::getBankId, bankId)
            .eq(userId != null, AnswerRecord::getUserId, userId));
    // ... 统计逻辑
}
```

**数据范围分析：**
- ADMIN：看到所有题库的所有学生答题记录
- TEACHER：看到自己有 MANAGE 权限的题库下的所有学生答题记录（**不限于自己班级的学生**）
- MANAGER：同 TEACHER
- 没有按 `classId` 过滤的参数
- 没有按教师关联学生过滤的机制

### 4.2 ExamTaskController.report()

```java
@GetMapping("/api/admin/exam-tasks/{id}/report")
public ApiResponse<Map<String, Object>> report(@PathVariable Long id) {
    permissionService.require(PermissionService.EXAM_TASK);  // 只要求 EXAM_TASK 权限

    ExamTask task = examTaskMapper.selectById(id);
    // ❌ 没有检查 task.getCreatedBy() == 当前用户
    // ❌ 没有检查当前用户是否关联该任务对应的班级

    List<ExamTaskUser> assignees = examTaskUserMapper.selectList(
        new LambdaQueryWrapper<ExamTaskUser>().eq(ExamTaskUser::getTaskId, id));
    List<ExamRecord> records = examRecordMapper.selectList(
        new LambdaQueryWrapper<ExamRecord>().eq(ExamRecord::getTaskId, id));

    // 返回所有考生成绩明细，不区分数据范围
    // ...
}
```

**数据范围分析：**
- 任何有 `EXAM_TASK` 权限的用户都能查看任何考试任务的成绩报告
- **没有做 `createdBy` 过滤**：教师A可以查看教师B创建的考试任务报告
- **没有做班级关联检查**：即使考试任务指定了 `classId`，也不检查当前教师是否关联该班级
- `bankAccessService.requireManageBank()` 在 `create/update/delete/publish/close` 方法中调用了，但 **`report()` 方法没有调用**

### 4.3 ExamTaskController 的其他接口数据范围

| 接口 | 权限检查 | 题库权限检查 | 创建者过滤 | 班级过滤 |
|------|----------|-------------|-----------|---------|
| `listAdmin` (列表) | `EXAM_TASK` | ❌ 无 | ❌ 无 | ❌ 无 |
| `getAdmin` (详情) | `EXAM_TASK` | ❌ 无 | ❌ 无 | ❌ 无 |
| `create` (创建) | `EXAM_TASK` | ✅ `requireManageBank` | N/A | N/A |
| `update` (编辑) | `EXAM_TASK` | ✅ `requireManageBank` | ❌ 无 | ❌ 无 |
| `publish` (发布) | `EXAM_TASK` | ✅ `requireManageBank` | ❌ 无 | ❌ 无 |
| `close` (关闭) | `EXAM_TASK` | ✅ `requireManageBank` | ❌ 无 | ❌ 无 |
| `delete` (删除) | `EXAM_TASK` | ✅ `requireManageBank` | ❌ 无 | ❌ 无 |
| `report` (报告) | `EXAM_TASK` | ❌ 无 | ❌ 无 | ❌ 无 |

**关键问题：** 列表和详情接口完全没有数据范围过滤，教师能看到所有考试任务（包括其他教师创建的）。报告接口也没有任何数据范围限制。

### 4.4 DashboardController 的数据范围

```java
@GetMapping("/api/admin/dashboard/summary")
public ApiResponse<Map<String, Object>> summary() {
    List<Long> bankIds = bankAccessService.currentAuthorizedBankIds();
    // 题库数量、题目数量、试卷数量 — 按题库授权过滤
    // 用户数量 — ❌ 无过滤，查询所有 status=1 的用户
    long userCount = userMapper.selectCount(
        new LambdaQueryWrapper<User>().eq(User::getStatus, 1));
}
```

用户数量统计完全没有按租户或角色过滤。

---

## 五、综合评估

### 5.1 当前实现状态总结

| 功能模块 | 状态 | 说明 |
|----------|------|------|
| 教师角色定义 | ✅ 已定义 | V2 迁移脚本插入 TEACHER 角色 |
| 教师默认权限 | ✅ 已配置 | 10 个权限码（代码兜底） |
| 教师进入后台 | ✅ 已支持 | `isBackstage()` 和 `hasWorkspace()` 包含 TEACHER |
| 班级表结构 | ✅ 已建表 | `class` + `class_user` |
| 班级 CRUD | ✅ 已实现 | 完整的增删改查接口 |
| 班级-学生关联 | ✅ 已实现 | `class_user` 表 + 考试任务自动拉取班级学生 |
| 班级-教师关联 | ❌ 不存在 | 无表、无字段、无接口 |
| 教师管理班级 | ❌ 无权限 | 教师无 `USER_WRITE` 权限 |
| 数据范围隔离 | ❌ 不存在 | 教师能看到所有数据 |
| 考试任务按教师过滤 | ❌ 不存在 | `report()` 和 `listAdmin()` 无创建者过滤 |
| 统计按班级过滤 | ❌ 不存在 | `adminPracticeStatistics()` 无 `classId` 参数 |
| 教师查看自己班级学生 | ❌ 不存在 | 无"我的班级"教师端接口 |

### 5.2 缺失的能力

1. **班级-教师关联模型**：没有"教师负责哪些班级"的数据模型
2. **教师班级管理权限**：教师无法创建/编辑班级、管理班级成员
3. **数据范围隔离**：
   - 考试任务列表/详情/报告不按 `createdBy` 或班级关联过滤
   - 统计接口不按班级过滤
   - Dashboard 用户数无过滤
4. **教师端班级视图**：没有"我的班级"列表接口
5. **班级维度统计**：不支持按班级查看学生练习/考试情况

### 5.3 需要新增的表/字段/接口

#### 需要新增的表

```sql
-- 班级-教师关联表
CREATE TABLE class_teacher (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  class_id BIGINT NOT NULL,
  teacher_id BIGINT NOT NULL,  -- user_id (roleCode = TEACHER)
  assigned_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  UNIQUE KEY uk_class_teacher (class_id, teacher_id),
  INDEX idx_class_teacher_teacher (teacher_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='班级教师关联';
```

或者，更简洁的方案 — 给 `class_user` 表增加角色字段：

```sql
-- 方案二（推荐）：扩展现有 class_user 表
ALTER TABLE class_user ADD COLUMN role VARCHAR(20) NOT NULL DEFAULT 'STUDENT'
  COMMENT 'STUDENT(学生) | TEACHER(教师)' AFTER user_id;
-- 修改唯一索引
ALTER TABLE class_user DROP INDEX uk_class_user,
  ADD UNIQUE KEY uk_class_user_role (class_id, user_id, role);
```

#### 需要新增/修改的字段

| 表 | 字段 | 类型 | 说明 |
|----|------|------|------|
| `class_user` | `role` | VARCHAR(20) | `STUDENT` 或 `TEACHER`，区分班级成员身份 |
| `exam_task` | （已有 `created_by`） | BIGINT | 已存在，但查询时未使用 |

#### 需要新增的权限标识

| 权限码 | 名称 | 说明 |
|--------|------|------|
| `CLASS_MANAGE` | 班级管理 | 创建/编辑/删除班级，管理班级成员 |

#### 需要新增的接口

| 接口 | 方法 | 说明 |
|------|------|------|
| `GET /api/teacher/my-classes` | GET | 教师查看自己负责的班级列表 |
| `GET /api/teacher/classes/{id}/students` | GET | 教师查看自己班级的学生列表 |
| `GET /api/teacher/classes/{id}/statistics` | GET | 教师查看自己班级的学生练习/考试统计 |
| `GET /api/teacher/exam-tasks` | GET | 教师查看自己创建的考试任务（按 `createdBy` 过滤） |
| `GET /api/teacher/exam-tasks/{id}/report` | GET | 教师查看自己创建的考试任务报告 |

#### 需要修改的现有接口

| 接口 | 修改内容 |
|------|----------|
| `GET /api/admin/exam-tasks` | 增加 `createdBy` 过滤：非 ADMIN 用户只能看到自己创建的任务 |
| `GET /api/admin/exam-tasks/{id}/report` | 增加 `createdBy` 检查：非 ADMIN 只能查看自己创建的任务报告 |
| `GET /api/admin/exam-tasks/{id}` | 同上，增加创建者权限检查 |
| `PUT /api/admin/exam-tasks/{id}` | 同上 |
| `POST /api/admin/exam-tasks/{id}/publish` | 同上 |
| `POST /api/admin/exam-tasks/{id}/close` | 同上 |
| `DELETE /api/admin/exam-tasks/{id}` | 同上 |
| `GET /api/statistics/admin/practice/statistics` | 增加 `classId` 参数，支持按班级过滤学生数据 |
| `GET /api/statistics/admin/practice/records` | 增加 `classId` 参数 |
| `GET /api/admin/classes` | 教师只看到自己关联的班级（非 ADMIN） |
| `POST /api/admin/classes` | 给教师增加 `CLASS_MANAGE` 权限 |
| `PUT /api/admin/classes/{id}/members` | 教师可管理自己班级的成员 |

#### 教师角色需要追加的权限

```java
// PermissionService.java 中 TEACHER_DEFAULT_CODES 需要追加
private static final List<String> TEACHER_DEFAULT_CODES = Arrays.asList(
    DASHBOARD,
    QUESTION_BANK,
    KNOWLEDGE_POINT,
    QUESTION,
    PAPER,
    NOTICE,
    USER_READ,
    EXAM_TASK,
    EXAM_TASK_MANAGE,
    STUDENT_STATISTICS_VIEW,
    CLASS_MANAGE,           // ← 新增：班级管理
    // 不给 USER_WRITE，避免教师能管理所有用户
);
```

---

## 六、架构关系图

```
┌─────────────────────────────────────────────────────────┐
│                    权限体系三层模型                        │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  第一层：功能权限 (PermissionService)                     │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌──────────┐   │
│  │  ADMIN  │  │ MANAGER │  │ TEACHER │  │ STUDENT  │   │
│  │ 全部权限 │  │ 业务权限 │  │ 业务权限 │  │ 用户端权限│   │
│  └────┬────┘  └────┬────┘  └────┬────┘  └──────────┘   │
│       │            │            │                        │
│  第二层：租户隔离 (tenantId)                              │
│  ┌──────────────────────────────────────────────────┐   │
│  │  非 ADMIN 用户只能操作本租户数据                    │   │
│  └──────────────────────────────────────────────────┘   │
│       │            │            │                        │
│  第三层：题库授权 (user_question_bank)                    │
│  ┌──────────────────────────────────────────────────┐   │
│  │  VIEW (学生) / MANAGE (教师/管理员)               │   │
│  └──────────────────────────────────────────────────┘   │
│                                                         │
│  ❌ 缺失的第四层：班级/教师数据隔离                       │
│  ┌──────────────────────────────────────────────────┐   │
│  │  教师只能看到自己班级的学生数据                      │   │
│  │  教师只能管理自己创建的考试任务                      │   │
│  └──────────────────────────────────────────────────┘   │
│                                                         │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│                    班级关联模型                           │
├─────────────────────────────────────────────────────────┤
│                                                         │
│   ┌──────────┐         ┌──────────────┐                │
│   │  class   │ 1─────N │  class_user  │ N─────1        │
│   │  班级表   │────────│ 班级用户关联  │────────│       │
│   └──────────┘         └──────────────┘        ┌─────┐ │
│        │                    │                  │ user│ │
│        │                    │ (无 role 字段)    │ 用户 │ │
│        │                    └──────────────────└─────┘ │
│        │                                               │
│        │         ┌──────────────────┐                  │
│        │ 1─────N │   exam_task      │                  │
│        └────────│  class_id 字段    │                  │
│                 └──────────────────┘                  │
│                                                         │
│   ❌ 缺失：class_teacher 表或 class_user.role 字段      │
│   ❌ 缺失：教师 ←→ 班级 的正式关联                       │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 七、实施建议（优先级排序）

### P0（必须做）

1. **给 `class_user` 表增加 `role` 字段**，区分学生和教师成员
2. **考试任务接口增加 `createdBy` 数据过滤**，非 ADMIN 只能看/操作自己创建的任务
3. **`report()` 接口增加题库权限检查**（`requireManageBank`）

### P1（应该做）

4. **新增 `CLASS_MANAGE` 权限标识**，加入教师默认权限
5. **统计接口增加 `classId` 过滤参数**
6. **新增教师端"我的班级"接口**
7. **班级管理接口对教师开放**（`USER_READ` → `CLASS_MANAGE`）

### P2（可以做）

8. **Dashboard 用户数按租户过滤**
9. **班级成员管理界面区分学生/教师角色**
10. **教师首页 Dashboard 显示自己班级的关键指标**

# 题库项目部署指南（宝塔 Linux 面板）

> 服务器：42.192.113.16（宝塔面板）
> 数据库：MySQL 5.7.44（question_bank 数据库已存在）
> 部署路径：/www/wwwroot/question_bank/

---

## 一、本地打包

### 1.1 后端打包（解决 JDK 问题）

由于系统只有 JRE 没有 JDK，需要指定 JDK 路径：

```cmd
set JAVA_HOME=C:\Program Files\Java\jdk1.8.0_321
set PATH=%JAVA_HOME%\bin;%PATH%
cd "C:\Users\li\Desktop\question_bank-master\question_bank-master\backend"
mvn clean install -DskipTests
```

打包完成后，jar 包在：
```
C:\Users\li\Desktop\question_bank-master\question_bank-master\backend\target\question-bank-0.0.1-SNAPSHOT.jar
```

### 1.2 前端打包

```cmd
cd "C:\Users\li\Desktop\question_bank-master\question_bank-master\frontend"
npm run build
```

打包完成后，产物在：
```
C:\Users\li\Desktop\question_bank-master\question_bank-master\frontend\dist\
```

---

## 二、上传到服务器

### 2.1 使用宝塔面板文件管理器上传

1. 登录宝塔面板：`https://42.192.113.16:36628`
2. 点击左侧菜单 **文件**
3. 进入目录 `/www/wwwroot/`
4. 创建文件夹 `question_bank`（如果没有）
5. 上传文件：
   - 上传 `question-bank-0.0.1-SNAPSHOT.jar` 到 `/www/wwwroot/question_bank/`
   - 上传 `dist` 文件夹内容到 `/www/wwwroot/question_bank/dist/`

### 2.2 或使用 WinSCP 上传

```bash
# 上传后端 jar
scp "C:\Users\li\Desktop\question_bank-master\question_bank-master\backend\target\question-bank-0.0.1-SNAPSHOT.jar" root@42.192.113.16:/www/wwwroot/question_bank/

# 上传前端 dist
scp -r "C:\Users\li\Desktop\question_bank-master\question_bank-master\frontend\dist\*" root@42.192.113.16:/www/wwwroot/question_bank/dist/
```

---

## 三、数据库升级

### 3.1 在宝塔面板中执行 SQL

1. 点击左侧菜单 **数据库**
2. 找到 `question_bank` 数据库，点击 **管理**（或 phpMyAdmin）
3. 导入以下 SQL 文件（按顺序执行）：

```sql
-- 1. 升级 class_user 表（加 role 字段）
ALTER TABLE class_user ADD COLUMN role VARCHAR(16) NOT NULL DEFAULT 'STUDENT' COMMENT 'STUDENT(学生) | TEACHER(教师)' AFTER user_id;

-- 2. 升级 exam_task 表（加 task_type 字段）
ALTER TABLE exam_task ADD COLUMN task_type VARCHAR(16) NOT NULL DEFAULT 'EXAM' COMMENT 'EXAM(考试) | HOMEWORK(作业)' AFTER description;

-- 3. 插入班级管理菜单（如果还没有）
-- 先查看现有菜单
SELECT id, name, parent_id, sort_order FROM menu_item WHERE menu_type = 'ADMIN' ORDER BY sort_order;

-- 插入班级管理菜单（假设"题库业务"的 id 为 2）
INSERT INTO menu_item (parent_id, name, path, icon, permission_code, menu_kind, menu_type, status, sort_order) 
SELECT 2, '班级管理', '/admin/classes', 'el-icon-school', 'USER_READ', 'MENU', 'ADMIN', 1, COALESCE(MAX(sort_order), 0) + 1
FROM menu_item WHERE parent_id = 2 AND menu_type = 'ADMIN';
```

---

## 四、配置后端

### 4.1 创建 application.yml 配置文件

在 `/www/wwwroot/question_bank/` 创建 `application.yml`：

```yaml
server:
  port: 8080
  tomcat:
    max-http-form-post-size: 20MB
    max-swallow-size: 20MB

spring:
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/question_bank?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: question_bank
    password: 你的数据库密码
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      minimum-idle: 5
      maximum-pool-size: 20
      idle-timeout: 30000
      max-lifetime: 120000
      connection-timeout: 10000
  flyway:
    enabled: false

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

app:
  jwt-secret: question-bank-demo-secret-change-me-question-bank-demo-secret
  jwt-expire-hours: 24
  token-encryption-key: question-bank-token-encryption-key-change-me

deepseek:
  api-key: 
  base-url: https://api.deepseek.com
  model: deepseek-v4-pro
```

> 注意：将 `password` 替换为宝塔面板中 question_bank 数据库的实际密码

---

## 五、启动后端服务

### 5.1 使用宝塔终端启动

1. 点击左侧菜单 **终端**
2. 执行以下命令：

```bash
cd /www/wwwroot/question_bank
nohup java -jar question-bank-0.0.1-SNAPSHOT.jar --spring.config.location=application.yml > app.log 2>&1 &
```

### 5.2 查看是否启动成功

```bash
ps -ef | grep question-bank
tail -f /www/wwwroot/question_bank/app.log
```

如果看到 `Started QuestionBankApplication` 字样，说明启动成功。

### 5.3 使用宝塔进程管理器（推荐）

1. 点击左侧菜单 **进程管理器**（或安装 **Supervisor** 插件）
2. 添加守护进程：
   - 名称：question-bank
   - 启动用户：root
   - 运行目录：/www/wwwroot/question_bank
   - 启动命令：`java -jar question-bank-0.0.1-SNAPSHOT.jar --spring.config.location=application.yml`

---

## 六、配置 Nginx（前端 + API 代理）

### 6.1 在宝塔面板中添加网站

1. 点击左侧菜单 **网站**
2. 点击 **添加站点**
3. 填写：
   - 域名：你的域名（或 IP 地址）
   - 根目录：/www/wwwroot/question_bank/dist
   - PHP 版本：纯静态
4. 点击 **提交**

### 6.2 配置反向代理

1. 点击刚添加的站点 **设置**
2. 点击 **反向代理** 标签
3. 添加反向代理：
   - 代理名称：api
   - 目标 URL：`http://127.0.0.1:8080`
   - 发送域名：$host
   - 点击 **提交**

### 6.3 或手动编辑 Nginx 配置

在站点设置中点击 **配置文件**，添加：

```nginx
server {
    listen 80;
    server_name 42.192.113.16;  # 或你的域名

    # 前端静态文件
    location / {
        root /www/wwwroot/question_bank/dist;
        try_files $uri $uri/ /index.html;
    }

    # API 代理到后端
    location /api/ {
        proxy_pass http://127.0.0.1:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

### 6.4 重启 Nginx

在宝塔面板中点击 **服务** → **Nginx** → **重载配置**

---

## 七、验证部署

1. 浏览器访问：`http://42.192.113.16`
2. 应该能看到登录页面
3. 登录后测试：
   - 班级管理是否能正常显示
   - 练习统计是否正常
   - 章节练习是否显示知识点列表

---

## 八、常见问题

### 8.1 后端启动失败（端口被占用）

```bash
# 查看 8080 端口占用
netstat -tlnp | grep 8080
# 杀掉占用进程
kill -9 PID
```

### 8.2 数据库连接失败

检查 `application.yml` 中的数据库密码是否正确，在宝塔面板数据库页面查看。

### 8.3 前端页面空白

检查 Nginx 配置中 `root` 路径是否正确指向 dist 目录。

### 8.4 更新部署（下次更新代码时）

1. 本地重新打包（后端 + 前端）
2. 上传新的 jar 包和 dist 文件到服务器（覆盖旧文件）
3. 如果有数据库改动，执行对应的 SQL 脚本
4. 重启后端服务：
   ```bash
   ps -ef | grep question-bank | grep -v grep | awk '{print $2}' | xargs kill -9
   cd /www/wwwroot/question_bank
   nohup java -jar question-bank-0.0.1-SNAPSHOT.jar --spring.config.location=application.yml > app.log 2>&1 &
   ```
5. 刷新 Nginx 配置

---

## 九、快速更新脚本（保存到服务器）

在 `/www/wwwroot/question_bank/` 创建 `update.sh`：

```bash
#!/bin/bash
# 题库项目更新脚本

echo "停止旧服务..."
pkill -f question-bank-0.0.1-SNAPSHOT.jar
sleep 2

echo "备份旧版本..."
cp question-bank-0.0.1-SNAPSHOT.jar question-bank-0.0.1-SNAPSHOT.jar.bak

echo "启动新版本..."
nohup java -jar question-bank-0.0.1-SNAPSHOT.jar --spring.config.location=application.yml > app.log 2>&1 &

echo "重启 Nginx..."
nginx -s reload

echo "更新完成！"
```

赋予执行权限：
```bash
chmod +x /www/wwwroot/question_bank/update.sh
```

以后更新时只需上传新 jar 包，然后执行：
```bash
./update.sh
```
